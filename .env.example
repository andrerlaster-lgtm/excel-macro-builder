# Excel Macro Builder - environment configuration
#
# Required for AI generation. Without this, the app still runs fully:
# the form, validation, and structured specification export all work.
# Only the "Generate VBA" step is disabled, with an honest message.
#
# Get a key at https://platform.openai.com/api-keys
#
# IMPORTANT: A ChatGPT Plus/Team/Pro subscription does NOT include API
# credits. API usage is billed separately, per token, on your OpenAI
# platform billing account. Set a usage limit at
# https://platform.openai.com/account/limits before testing heavily.
OPENAI_API_KEY=
